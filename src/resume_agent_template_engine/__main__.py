#!/usr/bin/env python3
"""
Entry point for the Resume Agent Template Engine CLI when run as a module.

Usage:
    python -m resume_agent_template_engine [command] [options]
"""

from resume_agent_template_engine.cli import main

if __name__ == '__main__':
    main() 